# OceanLink Codex Rule

## 基础技术约束
1. 本项目技术栈固定为：
    - JDK 17
    - Spring Boot
    - Maven

2. 生成的 Java 代码必须满足：
    - 作者统一为：`guohao.lu`
    - 所有类、接口、枚举、方法、公共字段必须补充 Javadoc
    - Javadoc 注释必须使用中文
    - 注释内容应准确描述用途、参数、返回值、异常等信息

## 工具与命令约束
3. 当需要调用终端命令、脚本或执行本地操作时，必须优先使用 PowerShell 可直接执行或兼容的命令，不使用依赖 bash/zsh 特性的命令写法。

## 文档同步约束
4. 任何代码生成、修改、重构、模块新增、接口新增、配置变更，都必须同步检查并更新 `README.md`，确保 README 与当前实现保持一致，至少包含：
    - 功能说明
    - 模块说明
    - 启动方式
    - 配置说明
    - 测试说明
    - 关键变更记录（如适用）

## 开发方法约束
5. 必须遵循 Spec 驱动开发 + 测试驱动开发（TDD）。

6. 每个**服务域模块**下必须直接创建 `spec` 目录；若模块采用 COLA 等多模块架构，则只要求在承载该业务服务域的模块下创建 `spec` 目录，不要求每个子模块都重复创建。
   `spec` 目录内固定包含以下三个文件，且文件名必须带上本次需求前缀，命名格式固定为：
    - `<需求前缀>-user-case.md`：记录产品级功能点、用户场景、目标价值
    - `<需求前缀>-task.md`：记录开发级详细设计点、拆分任务、边界条件
    - `<需求前缀>-plan.md`：记录具体执行方案，包括但不限于：
        - DDL 设计
        - 类设计
        - 模块设计
        - 接口设计
        - 单元测试设计
        - 集成测试设计（如适用）
   说明：
    - `<需求前缀>` 应使用能够准确表达本次需求主题的业务名称，例如：`接单`、`库存同步`、`履约推单`
    - 同一模块下若并行存在多个需求，必须通过不同前缀区分 spec 文件，避免覆盖或混淆

7. 开发流程必须严格按以下顺序执行，且每一步都需要用户介入评审：
    - 第一步：先生成 `<需求前缀>-user-case.md`
    - 第二步：等待用户 review
    - 第三步：用户修改或确认后，再生成 `<需求前缀>-task.md`
    - 第四步：等待用户 review
    - 第五步：用户修改或确认后，再生成 `<需求前缀>-plan.md`
    - 第六步：等待用户 review
    - 第七步：用户修改或确认后，才允许开始生成正式代码
    - 第八步：按 TDD 实施，优先编写测试，再补充实现代码
    - 第九步：代码完成后同步更新 `README.md`

## 执行优先级约束
8. 当项目中存在 `SKILL` / `SKILLS` / `SKILL.md` / AGENTS 类说明时，优先遵循技能文件与项目代理规则；若与本规则冲突，则按以下优先级执行：
    - 第一优先级：系统 / 平台 / IDE 明确约束
    - 第二优先级：项目内 `SKILL.md` / AGENTS / 项目规则
    - 第三优先级：本规则
    - 第四优先级：默认生成习惯

## 代码生成默认要求
9. 所有生成内容默认要求：
    - 优先保证可读性、可维护性、可测试性
    - 遵循单一职责原则
    - 避免过度设计
    - 优先生成 Maven 标准目录结构
    - 测试代码与业务代码同步设计、同步交付
    - 若需求不明确，先补 spec，再进入实现
